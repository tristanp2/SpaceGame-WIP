#ifndef ENUM_H
#define ENUM_H

enum ObjectType{
    enum_player,
    enum_enemy,
    enum_asteroid,
    enum_bullet,
    enum_misc
};
enum GameState{
    enum_menu,
    enum_play,
    enum_dead,
    enum_quit
};
#endif
